- name: Build project
  ansible.builtin.shell: |
    export PATH="{{ ansible_user_dir }}/.local/bin:$PATH"
    bazelisk build --config=ci {{ "--config=release" if is_release else "" }} --//:browser="{{ browser }}" --//:channel="{{ channel }}" {{targets}}
  args:
    chdir: '{{ zuul_work_dir }}'
