- name: Test project
  ansible.builtin.shell: |
    export PATH="{{ ansible_user_dir }}/.local/bin:$PATH"
    bazelisk test --config=ci --//:browser="{{ browser }}" --//:channel="{{ channel }}" //...
  args:
    chdir: '{{ zuul_work_dir }}'
