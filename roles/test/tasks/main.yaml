- name: Test project
  ansible.builtin.shell: |
    export PATH="{{ ansible_user_dir }}/.local/bin:$PATH"
    bazelisk test --//:browser="{{ browser }}" --//:channel="{{ channel }}" //...
  args:
    chdir: '{{ zuul_work_dir }}'
