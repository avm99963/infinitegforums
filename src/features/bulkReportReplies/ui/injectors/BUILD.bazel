load("//tools:bazel/swc.bzl", "swc_pkg")
load("//tools:common_ts_project.bzl", "common_ts_project")

common_ts_project(
    name = "injectors",
    allow_js = True,
    declaration = True,
    source_map = True,
    transpiler = swc_pkg,
    tsconfig = "//:tsconfig_pkg",
    visibility = [
        "//src/features/bulkReportReplies/infrastructure:__subpackages__",
        "//src/features/bulkReportReplies/presentation:__subpackages__",
    ],
)
