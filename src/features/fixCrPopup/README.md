# Fix canned response popup

Feature that fixes the styling of the Canned Responses popup in the message
editor.

Right now, the only fix it makes is to ensure that the width of the popup is
not very large (for long CR names, the popup can have a width larger than the
width of the viewport).
