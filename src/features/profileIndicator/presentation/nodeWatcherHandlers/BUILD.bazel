load("//tools:bazel/swc.bzl", "swc_pkg")
load("//tools:common_ts_project.bzl", "common_ts_project")

common_ts_project(
    name = "nodeWatcherHandlers",
    allow_js = True,
    declaration = True,
    source_map = True,
    transpiler = swc_pkg,
    tsconfig = "//:tsconfig_pkg",
    visibility = [
        "//src:entrypoint",
    ],
    deps = [
        "//src/features/profileIndicator/core",
        "//src/infrastructure/presentation/nodeWatcher/handlers",
        "//src/presentation/nodeWatcher",
        "//src/services/i18n/chrome",
        "//src/services/options",
    ],
)
