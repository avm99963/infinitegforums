load("//tools:bazel/swc.bzl", "swc_pkg")
load("//tools:common_ts_project.bzl", "common_ts_project")

common_ts_project(
    name = "modifierMatcher",
    allow_js = True,
    declaration = True,
    source_map = True,
    transpiler = swc_pkg,
    tsconfig = "//:tsconfig_pkg",
    visibility = [
        "//src/xhrInterceptor:__subpackages__",
    ],
    deps = [
        "//src/common/options",
        "//src/services/options",
    ],
)
