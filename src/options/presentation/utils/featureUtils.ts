import { msg } from '@lit/localize';

export const COMMUNITY_CONSOLE_TAG = msg('Community Console', {
  desc: 'Tag shown in the options page for features that work in the Community Console.',
});
export const TAILWIND_BASIC_TAG = msg('Tailwind Basic', {
  desc: 'Tag shown in the options page for features that work in Tailwind Basic.',
});
